<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

    <title><%= title %> :: Index</title>

    <!-- Bootstrap core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <link href="css/general.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><img class="logo" title="<%= title %>"></a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="active"><a href="/">Home</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>

    <div class="container">

      <div class="center" id="search">
        <h1>Welcome to <%= title %></h1>
        <p class="lead">Fido is ready to fetch some results<br></p>
        <img class="fido">
        <form method="POST">
          <input type="text" id="query" placeholder="Search for a restaurant" autofocus><span id="loading" class="glyphicon glyphicon-refresh glyphicon-refresh-animate"></span>
        </form>
      </div>
    </div>
    <div class="container" id="results">
      <div class="row">
        <p align="center">Showing 1st 50 results</p>
        <div class="col-xs-6 col-sm-4" id="yelp">
          <table class="table" id="yelpTable">
            <caption>Yelp</caption>
            <thead>
              <tr>
                <th>#</th>
                <th>Phone</th>
                <th>Zip</th>
              </tr>
            </thead>
            <tbody>
            </tbody>
          </table>
        </div>
        <div class="col-xs-6 col-sm-4" id="combo">
          <table class="table" id="comboTable">
            <caption>Combo</caption>
            <thead>
              <tr>
                <th>#</th>
                <th>Phone</th>
                <th>Zip</th>
                <th>What3Words</th>
              </tr>
            </thead>
            <tbody>
            </tbody>
          </table>
        </div>
        <div class="col-xs-6 col-sm-4" id="foursquare">
          <table class="table" id="foursquareTable">
            <caption>Foursquare</caption>
            <thead>
              <tr>
                <th>#</th>
                <th>Phone</th>
                <th>Zip</th>
              </tr>
            </thead>
            <tbody>
            </tbody>
          </table>
        </div>
      </div>
      <table>

      </table>
    </div>


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jqueryui/1.11.4/jquery-ui.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="js/index.js"></script>
  </body>
</html>
